 {
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "python3 -m pip install --upgrade pip && python3 -m pip install --no-cache-dir --prefer-binary -r requirements.txt && python3 manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "python3 manage.py migrate && gunicorn to7fabackend.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/admin/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 