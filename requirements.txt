django==4.2.13
djangorestframework==3.16.0
djangorestframework-simplejwt==5.5.0
django-cors-headers==4.7.0
# mysqlclient provides the MySQLdb module directly (Django's mysql backend imports it by
# default) - no compatibility shim needed. pymysql (a pure-Python alternative requiring
# pymysql.install_as_MySQLdb() to masquerade as MySQLdb) was previously also listed here
# with no code anywhere using it; removed (Phase 2) rather than supporting two drivers.
mysqlclient==2.2.7
pillow==11.2.1
python-dotenv==1.0.1
cryptography
channels==4.0.0
channels-redis==4.2.0
django-redis==5.4.0
celery==5.3.6
# Testing
# NOTE: pytest-django==4.5.2 predates pytest 9 by several years; pin pytest to the
# version this was verified against for reproducibility. Compatibility between the two
# is unverified and worth a closer look in a later phase.
pytest==9.1.1
pytest-django==4.5.2
# Push notification dependencies
requests>=2.25.1
